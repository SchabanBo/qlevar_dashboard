import 'package:flutter/material.dart';

class DashboardChildView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Dashboard'),
    );
  }
}
